this project has all of these :
Step 2: Controlled Components: Create states to store the values of these inputs. Use the onChange event to update the state as the user types.
Step 3: Operation Logic: Create a state for the result. Implement a button "Calculate Sum" that parses the input strings into numbers and adds them.
Step 4: Reset Feature: Add a "Clear" button that resets all inputs and the result state to zero or empty strings.